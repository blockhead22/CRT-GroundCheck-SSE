# Legacy Root Scratch Archive

This folder holds old one-off helper scripts that were useful enough to keep
for reference, but are not live product, lab, or Workbench entry points.

The original root-level temporary cleanup script that wiped local CRT databases
was removed from the working tree during the July 2026 repo cleanup. Recover it
from Git history only if you intentionally need to inspect that old behavior.
